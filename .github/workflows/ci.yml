name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: finanzas_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 5s --health-timeout 5s --health-retries 10
    env:
      TEST_MIGRATION_DATABASE_URL: postgres://postgres:postgres@localhost:5432/finanzas_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Tipos
        run: npm run typecheck
      - name: Lint
        run: npm run lint
      - name: Formato
        run: npm run format:check
      - name: Tests (incluye aislamiento multiempresa y auditoría contra PostgreSQL real)
        run: npm test
      - name: Build
        run: npm run build
        env:
          DATABASE_URL: postgres://finanzas_app:x@localhost:5432/finanzas_test
          BETTER_AUTH_SECRET: ci-build-secret-ci-build-secret-123
          BETTER_AUTH_URL: http://localhost:3000
